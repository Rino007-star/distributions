import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "RINO - Innovative Software Solutions",
  description: "RINO crafts exceptional digital experiences through cutting-edge software development, bringing your ideas to life with precision, creativity, and technical excellence.",
  keywords: ["software development", "web development", "mobile apps", "custom software", "RINO"],
  authors: [{ name: "RINO" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
